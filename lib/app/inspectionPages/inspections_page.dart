import 'package:flutter/material.dart';

class InspectionsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Column(
      children: <Widget>[
        Center(
          child: Text('InspectionsPage'),
        ),
      ],
    );
  }
}
